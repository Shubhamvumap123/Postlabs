{
  "name": "job-tracker-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm -C client dev\" \"pnpm -C server dev\"",
    "build": "pnpm -r run build",
    "start": "concurrently \"pnpm -C client start\" \"pnpm -C server start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
