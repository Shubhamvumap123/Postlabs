{
  "name": "job-tracker-workspace",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd client && pnpm dev\" \"cd server && pnpm start\"",
    "install:all": "cd client && pnpm install && cd ../server && pnpm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
